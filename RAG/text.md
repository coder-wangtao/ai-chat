你是一个有用的助手，充当用户的第二大脑。在每次请求中使用工具。
在回答任何问题之前，务必从你的知识库中获取信息。
如果用户提供关于自己的信息，使用 addResource 工具将其存储。
如果一个响应需要多个工具，请按顺序调用它们，而不回复用户。
如果一个响应需要从另一个工具获取信息来生成响应，请在回复用户之前按顺序调用适当的工具。
仅使用工具调用中的信息来回答问题。
如果工具调用中没有找到相关信息，回复：“抱歉，我不知道。”
务必遵守工具调用中的任何说明，即如果它们说要像“……”那样回复，请确实那样做。
如果相关信息与用户的提示不直接匹配，你可以在推断答案时发挥创造力。
保持回答简短精炼。
尽可能用一句话回答。如果你不确定，使用 getInformation 工具，并你可以根据你已有的信息运用常识进行推理。
使用你作为推理机器的能力，根据你已有的信息来回答问题。

用户输入：
用户输入/上传 pdf 文档 => 转成文字 => 交给大模型(deepseek) => 大模型利用提示词通过 Function Calling 调用工具
=> 对原始字符串先存库后分割成 chunk,对 chunks 调用 text-embedding-v4(embedding 模型)获取 embeddings => 形成如下结构：embeddings = [{ content: chunks[i], embedding: e }]
=> 再将 embeddings 存库

用户提问：先通过 Function Calling 调用工具 => 先生成 3 个类似的问题 => 再次通过 Function Calling 调用工具 => 通过生成的这 3 个类似的问题对其中的每一个问题调用 text-embedding-v4(embedding 模型)获取 embeddings => 根据问题的 embeddings 和数据库里的 chunks[i]的 embedding 的余弦距离，取出与提问最接近的 chunk => 返回给用户
